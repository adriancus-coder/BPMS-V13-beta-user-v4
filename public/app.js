const socket = io();
const $ = (id) => document.getElementById(id);

let currentEvent = null;
let currentVolume = 70;
let currentMuted = false;
let selectedEntryId = null;
let sourceEditLock = false;
let activeTab = 'live';
let lastManualEnterAt = 0;
let screenWakeLock = null;
window.isRecognitionRunning = false;

const ALL_LANGS = {
  ro: 'Română', no: 'Norvegiană', ru: 'Rusă', uk: 'Ucraineană', en: 'Engleză', es: 'Spaniolă',
  fr: 'Franceză', de: 'Germană', it: 'Italiană', pt: 'Portugheză', pl: 'Poloneză', tr: 'Turcă', ar: 'Arabă', fa: 'Persană', hu: 'Maghiară'
};

let audioState = { stream:null, context:null, source:null, gainNode:null, preampNode:null, analyser:null, destination:null, meterFrame:null, recorder:null, running:false, busy:false, uploadQueue:[], chunks:[], chunkTimer:null, mimeType:'', monitorGainNode:null, monitorEnabled:false };

function createLanguageChecks() {
  const box = $('languageChecks'); if (!box) return;
  box.innerHTML = Object.entries(ALL_LANGS).map(([value,label]) => `<label><input type="checkbox" value="${value}" ${['no','en'].includes(value)?'checked':''}> ${label}</label>`).join('');
}
function selectedLangs() { return Array.from(document.querySelectorAll('#languageChecks input[type="checkbox"][value]:checked')).map((i) => i.value); }
function setStatus(text) { const el = $('recognitionStatus'); if (el) el.textContent = text; }
function setOnAirState(isOn) { const badge = $('onAirBadge'); if (!badge) return; badge.textContent = isOn ? 'On-Air' : 'Off-Air'; badge.className = isOn ? 'status-pill active' : 'status-pill'; }
async function enableScreenWakeLock() { try { if (!('wakeLock' in navigator) || document.visibilityState !== 'visible' || screenWakeLock) return; screenWakeLock = await navigator.wakeLock.request('screen'); screenWakeLock.addEventListener('release',()=>{ screenWakeLock=null; }); } catch (err) { console.error('Wake Lock error:', err?.message || err); } }
async function disableScreenWakeLock() { try { if (screenWakeLock) { await screenWakeLock.release(); screenWakeLock = null; } } catch (err) { console.error('Wake Lock release error:', err?.message || err); } }
function switchTab(tabName) { activeTab = tabName; document.querySelectorAll('.tab-btn').forEach(btn=>btn.classList.toggle('active', btn.dataset.tab===tabName)); document.querySelectorAll('.tab-panel').forEach(panel=>panel.classList.toggle('active', panel.id===`tab-${tabName}`)); }
function initTabs() { document.querySelectorAll('.tab-btn').forEach(btn=>btn.addEventListener('click',()=>switchTab(btn.dataset.tab))); switchTab('live'); }
function updateGlossaryMode() { const mode = $('glossaryMode')?.value || 'translation'; if ($('translationGlossaryFields')) $('translationGlossaryFields').style.display = mode==='translation'?'block':'none'; if ($('sourceCorrectionFields')) $('sourceCorrectionFields').style.display = mode==='source'?'block':'none'; }
function escapeHtml(text) { return String(text || '').replaceAll('&','&amp;').replaceAll('<','&lt;').replaceAll('>','&gt;').replaceAll('"','&quot;'); }
function formatDateTime(value) { if (!value) return '-'; return new Date(value).toLocaleString(); }
function renderActiveEventBadge(event) { const badge=$('activeEventBadge'); const opened=$('openedEventBadge'); if (!badge) return; if (!event) { badge.textContent='Niciun eveniment activ'; badge.className='status-pill'; if (opened) opened.textContent='Niciun eveniment deschis'; return; } const extra = event.scheduledAt ? ` · ${formatDateTime(event.scheduledAt)}` : ''; badge.textContent = event.isActive ? `Live: ${event.name || 'Eveniment'}${extra}` : `Live: alt eveniment${extra}`; badge.className = event.isActive ? 'status-pill active' : 'status-pill'; if (opened) opened.textContent = `Deschis: ${event.name || 'Eveniment'}${extra}`; }
function renderParticipantStats(stats = {}) { const summaryEl=$('participantStatsSummary'); const listEl=$('participantStatsList'); const uniqueCount=Number(stats.uniqueCount || stats.total || 0); const sorted=(Array.isArray(stats.languages)?stats.languages:Object.entries(stats.byLanguage||{}).map(([lang,count])=>({lang,count}))).sort((a,b)=>Number(b.count||0)-Number(a.count||0)); if (summaryEl) summaryEl.textContent = uniqueCount===1 ? '1 participant unic' : `${uniqueCount} participanți unici`; if (listEl) listEl.innerHTML = sorted.length ? sorted.map(item=>`<div class="history-item">${escapeHtml(ALL_LANGS[item.lang] || item.lang.toUpperCase())}: ${item.count}</div>`).join('') : '<div class="small">Niciun participant conectat.</div>'; }
function resetParticipantStats() { renderParticipantStats({ uniqueCount: 0, languages: [] }); }
function getEntryById(entryId) { return (currentEvent?.transcripts || []).find((x) => x.id === entryId) || null; }
function fillGlossaryLangs(targetLangs = []) { const select = $('glossaryLang'); if (!select) return; select.innerHTML=''; targetLangs.forEach((lang) => { const opt=document.createElement('option'); opt.value=lang; opt.textContent=ALL_LANGS[lang] || lang.toUpperCase(); select.appendChild(opt); }); }
function copyField(id, buttonId) { const value = ($(id)?.value || '').trim(); if (!value) return; navigator.clipboard.writeText(value).then(() => { const btn=$(buttonId); if (!btn) return; const old=btn.textContent; btn.textContent='Copied'; setTimeout(()=>btn.textContent=old,1200); }).catch(()=>setStatus('Nu am putut copia linkul.')); }
async function copyTextQuick(text, button) { const clean=String(text || '').trim(); if (!clean) return; try { await navigator.clipboard.writeText(clean); if (!button) return; const old=button.textContent; button.textContent='Copied'; setTimeout(()=>button.textContent=old,1200); } catch { setStatus('Nu am putut copia textul.'); } }
function buildScheduledAt() { const date=$('eventDate')?.value; const time=$('eventTime')?.value; if (!date) return null; return time ? `${date}T${time}:00` : `${date}T00:00:00`; }
function openInlineEditor(entryId) { selectedEntryId = entryId; sourceEditLock=true; document.querySelectorAll('.entry.active').forEach(el=>el.classList.remove('active')); document.querySelectorAll('.inline-editor.open').forEach(el=>el.classList.remove('open')); const card=document.querySelector(`[data-entry-id="${entryId}"]`); if (!card) return; card.classList.add('active'); const editor=card.querySelector('.inline-editor'); if (editor) editor.classList.add('open'); requestAnimationFrame(()=>{ const textarea=card.querySelector('.inline-source'); if (!textarea) return; textarea.focus(); textarea.selectionStart=textarea.value.length; textarea.selectionEnd=textarea.value.length; }); }
function closeInlineEditors() { document.querySelectorAll('.entry.active').forEach(el=>el.classList.remove('active')); document.querySelectorAll('.inline-editor.open').forEach(el=>el.classList.remove('open')); selectedEntryId=null; sourceEditLock=false; }
function renderEntry(entry) { const list=$('transcriptList'); if (!list) return; const div=document.createElement('div'); div.className='entry'; div.dataset.entryId=entry.id; const editedBadge=entry.edited?'<div class="small badge-inline">Corectat</div>':''; const sourceLabel=ALL_LANGS[entry.sourceLang] || entry.sourceLang?.toUpperCase() || 'Origine'; const translations=Object.entries(entry.translations || {}).map(([lang,text])=>`<div class="trans" data-lang="${lang}"><b>${lang.toUpperCase()}:</b> ${escapeHtml(text)}</div>`).join(''); div.innerHTML = `<div class="entry-meta">${formatDateTime(entry.createdAt)}</div><div class="entry-head row space-between"><div class="transcript-source-row"><div class="orig"><b>${sourceLabel}:</b> ${escapeHtml(entry.original)}</div><button class="entry-copy-btn" type="button">Copy fraza</button></div>${editedBadge}</div>${translations}<div class="inline-editor"><div class="small">Corectează și retradu toate limbile</div><textarea class="inline-source">${escapeHtml(entry.original)}</textarea><div class="inline-actions"><button class="btn-green inline-save" type="button">Retradu toate</button><button class="btn-dark inline-close" type="button">Închide</button></div></div>`; div.addEventListener('click',(e)=>{ if (e.target.closest('button') || e.target.closest('textarea')) return; openInlineEditor(entry.id); }); div.querySelector('.entry-copy-btn')?.addEventListener('click',(e)=>{ e.stopPropagation(); copyTextQuick(entry.original, e.currentTarget); }); div.querySelector('.inline-save')?.addEventListener('click',(e)=>{ e.stopPropagation(); saveInlineSource(entry.id); }); div.querySelector('.inline-close')?.addEventListener('click',(e)=>{ e.stopPropagation(); closeInlineEditors(); }); div.querySelector('.inline-source')?.addEventListener('keydown',(e)=>{ if (e.key==='Enter' && !e.shiftKey) { e.preventDefault(); saveInlineSource(entry.id); } }); list.prepend(div); }
function updateEntry({ entryId, lang, text }) { const entry=document.querySelector(`[data-entry-id="${entryId}"]`); if (!entry) return; let line=entry.querySelector(`.trans[data-lang="${lang}"]`); if (!line) { line=document.createElement('div'); line.className='trans'; line.dataset.lang=lang; entry.insertBefore(line, entry.querySelector('.inline-editor')); } line.innerHTML=`<b>${lang.toUpperCase()}:</b> ${escapeHtml(text)}`; }
function updateSourceEntry({ entryId, sourceLang, original, translations }) { const entry=document.querySelector(`[data-entry-id="${entryId}"]`); if (!entry) return; const orig=entry.querySelector('.orig'); if (orig) orig.innerHTML = `<b>${ALL_LANGS[sourceLang] || sourceLang?.toUpperCase() || 'Origine'}:</b> ${escapeHtml(original)}`; const textarea=entry.querySelector('.inline-source'); if (textarea) textarea.value=original; Object.entries(translations || {}).forEach(([lang,text])=>updateEntry({ entryId, lang, text })); const actual=getEntryById(entryId); if (actual) { actual.sourceLang=sourceLang; actual.original=original; actual.translations=translations; actual.edited=true; } }
function saveInlineSource(entryId) { if (!currentEvent) return; const card=document.querySelector(`[data-entry-id="${entryId}"]`); const textarea=card?.querySelector('.inline-source'); if (!textarea) return; const text=textarea.value.trim(); if (!text) return; selectedEntryId=entryId; sourceEditLock=false; socket.emit('admin_update_source', { eventId: currentEvent.id, entryId, sourceText: text }); closeInlineEditors(); }
function renderEventList(events = [], activeEventId = null, openedEventId = null) { const box=$('eventList'); if (!box) return; box.innerHTML=''; if (!events.length) { box.innerHTML='<div class="small">Nu există încă evenimente.</div>'; return; } events.forEach((event)=>{ const card=document.createElement('div'); card.className=`event-card${event.id===activeEventId?' active':''}${event.id===openedEventId?' opened':''}`; const langs=(event.targetLangs || []).map(lang=>ALL_LANGS[lang] || lang.toUpperCase()).join(', '); card.innerHTML=`<div class="name">${escapeHtml(event.name || 'Eveniment nou')}</div>${event.isActive?'<div class="badge">Live acum</div>':''}<div class="meta">Programat: ${escapeHtml(formatDateTime(event.scheduledAt || event.createdAt))}</div><div class="meta">Limbi: ${escapeHtml(langs || '-')}</div><div class="meta">Texte: ${event.transcriptCount || 0} · Mod: ${escapeHtml(event.mode || 'live')}</div><div class="actions"><button class="btn-dark" data-action="open" data-id="${event.id}">Deschide</button><button class="btn-blue" data-action="activate" data-id="${event.id}">Pune live</button><button data-action="delete" data-id="${event.id}">Șterge</button></div>`; box.appendChild(card); }); }
async function refreshEventList() { try { const res=await fetch('/api/events'); const data=await res.json(); if (!data.ok) return; renderEventList(data.events || [], data.activeEventId || null, currentEvent?.id || null); } catch (err) { console.error(err); } }
async function refreshSongLibrary() { if (!currentEvent) return; const list=$('songLibraryList'); if (!list) return; try { const res=await fetch(`/api/events/${currentEvent.id}/song/library`); const data=await res.json(); if (!data.ok) return; list.innerHTML = (data.songs || []).length ? data.songs.map(song=>`<div class="event-card"><div class="name">${escapeHtml(song.title || 'Fără titlu')}</div><div class="meta">${escapeHtml(formatDateTime(song.createdAt))}</div><div class="actions"><button class="btn-dark" data-song-action="load" data-song-id="${song.id}">Load</button><button data-song-action="delete" data-song-id="${song.id}">Șterge</button></div></div>`).join('') : '<div class="small">Nu există cântări salvate.</div>'; } catch (e) { console.error(e); } }
function renderSongHistory(songState) { const box=$('songHistoryList'); if (!box) return; const history = songState?.history || []; box.innerHTML = history.length ? history.slice().reverse().map(item=>`<div class="history-item">${escapeHtml(item.translations?.[currentEvent?.targetLangs?.[0] || 'no'] || Object.values(item.translations || {})[0] || '')}</div>`).join('') : '<div class="small">Istoricul cântării este gol.</div>'; }
function renderSongStatus(songState) { const el=$('songStatus'); if (!el) return; if (!songState?.blocks?.length) { el.textContent='Nicio cântare încărcată.'; return; } el.textContent = `Strofă ${songState.currentIndex + 1} din ${songState.blocks.length}${songState.title ? ' · ' + songState.title : ''}`; renderSongHistory(songState); }
async function syncSpeedToEvent() { if (!currentEvent) return; const speed=$('speed')?.value || 'balanced'; try { const res=await fetch(`/api/events/${currentEvent.id}/settings`, { method:'POST', headers:{ 'Content-Type':'application/json' }, body: JSON.stringify({ speed }) }); const data=await res.json(); if (!data.ok) return; currentEvent=data.event; setStatus(`Mod traducere: ${speed}`); } catch (err) { console.error(err); } }
async function openEventById(eventId) { if (!eventId) return; try { const res=await fetch(`/api/events/${eventId}`); const data=await res.json(); if (!data.ok) return; currentEvent=data.event; $('adminCode').textContent=currentEvent.adminCode; $('participantLink').value=currentEvent.participantLink || ''; $('displayLink').value=currentEvent.displayLink || currentEvent.songLink || ''; $('qrImage').src=currentEvent.qrCodeDataUrl || ''; $('speed').value=currentEvent.speed || 'balanced'; currentVolume=currentEvent.audioVolume; currentMuted=currentEvent.audioMuted; $('volumeRange').value=String(currentVolume); $('transcriptList').innerHTML=''; (currentEvent.transcripts || []).forEach(renderEntry); fillGlossaryLangs(currentEvent.targetLangs || []); renderActiveEventBadge(currentEvent); renderSongStatus(currentEvent.songState); closeInlineEditors(); $('partialTranscript').textContent='Aștept propoziția completă...'; socket.emit('join_event', { eventId: currentEvent.id, role:'admin', code: currentEvent.adminCode }); await refreshEventList(); await refreshSongLibrary(); setStatus(`Ai deschis evenimentul: ${currentEvent.name || 'Eveniment'}.`); switchTab('live'); } catch (err) { console.error(err); } }
async function createEvent() { const name=$('eventName')?.value.trim() || 'Eveniment nou'; const sourceLang=$('sourceLang')?.value; const targetLangs=selectedLangs(); const scheduledAt=buildScheduledAt(); if (!targetLangs.length) return alert('Alege cel puțin o limbă.'); if (targetLangs.includes(sourceLang)) return alert('Scoate limba de origine din lista limbilor traduse.'); const res=await fetch('/api/events', { method:'POST', headers:{ 'Content-Type':'application/json' }, body: JSON.stringify({ name, speed:$('speed')?.value || 'balanced', sourceLang, targetLangs, scheduledAt }) }); const data=await res.json(); if (!data.ok) return alert(data.error || 'Nu am putut crea evenimentul.'); await openEventById(data.event.id); }
async function setActiveEvent() { if (!currentEvent) return; const res=await fetch(`/api/events/${currentEvent.id}/activate`, { method:'POST' }); const data=await res.json(); if (!data.ok) return alert(data.error || 'Nu am putut seta evenimentul activ.'); currentEvent=data.event; renderActiveEventBadge(currentEvent); setStatus('Evenimentul public a fost actualizat.'); }
async function setMode(mode) { if (!currentEvent) return; const res = await fetch(`/api/events/${currentEvent.id}/mode`, { method:'POST', headers:{ 'Content-Type':'application/json' }, body: JSON.stringify({ mode }) }); const data=await res.json(); if (data.ok) { currentEvent=data.event; renderActiveEventBadge(currentEvent); setStatus(`Mod setat: ${mode}`); } }
async function loadSong() { if (!currentEvent) return alert('Deschide un eveniment.'); const title=$('songTitle')?.value.trim() || ''; const text=$('songText')?.value.trim() || ''; if (!text) return alert('Pune textul cântării.'); const res = await fetch(`/api/events/${currentEvent.id}/song/load`, { method:'POST', headers:{ 'Content-Type':'application/json' }, body: JSON.stringify({ title, text, saveToLibrary: !!$('saveSongToLibrary')?.checked }) }); const data=await res.json(); if (!data.ok) return alert(data.error || 'Nu am putut încărca cântarea.'); currentEvent=data.event; renderSongStatus(data.songState); await refreshSongLibrary(); setStatus('Cântarea a fost tradusă și trimisă.'); }
async function songAction(path) { if (!currentEvent) return; const res=await fetch(`/api/events/${currentEvent.id}${path}`, { method:'POST' }); const data=await res.json(); if (data.ok) { if (data.event) currentEvent=data.event; if (data.songState) { currentEvent.songState=data.songState; renderSongStatus(data.songState); } } else alert(data.error || 'Eroare song mode.'); }
async function loadAudioInputs(keepValue = true) { const select=$('audioInput'); if (!select) return; const previous=keepValue ? select.value : ''; select.innerHTML=''; try { const devices=await navigator.mediaDevices.enumerateDevices(); const inputs=devices.filter((d)=>d.kind==='audioinput'); if (!inputs.length) { const o=document.createElement('option'); o.textContent='Niciun input audio'; o.value=''; select.appendChild(o); return; } inputs.forEach((d)=>{ const o=document.createElement('option'); o.value=d.deviceId; o.textContent=d.label || 'Input audio'; select.appendChild(o); }); if (previous && inputs.some((d)=>d.deviceId===previous)) select.value=previous; } catch (err) { console.error(err); setStatus('Nu am putut citi sursele audio.'); } }
async function destroyAudioPipeline() { audioState.running=false; if (audioState.chunkTimer) clearTimeout(audioState.chunkTimer); audioState.chunkTimer=null; audioState.chunks=[]; audioState.mimeType=''; if (audioState.recorder && audioState.recorder.state!=='inactive') { try { audioState.recorder.stop(); } catch (_) {} } audioState.recorder=null; if (audioState.meterFrame) cancelAnimationFrame(audioState.meterFrame); audioState.meterFrame=null; if (audioState.stream) audioState.stream.getTracks().forEach((t)=>t.stop()); audioState.stream=null; if (audioState.context) await audioState.context.close().catch(()=>{}); audioState.context=null; audioState.source=null; audioState.gainNode=null; audioState.preampNode=null; audioState.analyser=null; audioState.monitorGainNode=null; audioState.destination=null; audioState.busy=false; audioState.uploadQueue=[]; $('audioLevel').value=0; setOnAirState(false); }
function sliderToGain(value) { const v=Math.max(0, Number(value || 100)); return Math.pow(v / 100, 2); }
function updateInputGain() { const value=Number($('inputGainRange')?.value || 100); const gain=sliderToGain(value); $('inputGainLabel').textContent=`${value}% · ${gain.toFixed(1)}x`; if (audioState.preampNode) audioState.preampNode.gain.value=gain; }
function updateMonitorGain() { const enabled=!!$('monitorAudioBox')?.checked; const value=Number($('monitorGainRange')?.value || 0); const gain=enabled ? sliderToGain(value) : 0; $('monitorGainLabel').textContent=`${value}% · ${gain.toFixed(1)}x`; if (audioState.monitorGainNode) audioState.monitorGainNode.gain.value=gain; }
function startMeterLoop() { if (!audioState.analyser) return; const data=new Uint8Array(audioState.analyser.fftSize); const draw=()=>{ if (!audioState.analyser) return; audioState.analyser.getByteTimeDomainData(data); let sumSquares=0; for (let i=0;i<data.length;i++) { const normalized=(data[i]-128)/128; sumSquares += normalized*normalized; } const rms=Math.sqrt(sumSquares/data.length); const db=20*Math.log10(Math.max(rms,0.00001)); const level=Math.max(0, Math.min(100, Math.round(((db+60)/60)*100))); $('audioLevel').value=level; setStatus(audioState.running ? 'Traduce din sursa selectată.' : 'Semnal audio OK.'); audioState.meterFrame=requestAnimationFrame(draw); }; draw(); }
async function createAudioPipeline() { const deviceId=$('audioInput')?.value; await destroyAudioPipeline(); audioState.stream = await navigator.mediaDevices.getUserMedia({ audio: deviceId ? { deviceId:{ exact:deviceId }, channelCount:2, sampleRate:48000, sampleSize:16, echoCancellation:false, noiseSuppression:false, autoGainControl:false } : { channelCount:2, sampleRate:48000, sampleSize:16, echoCancellation:false, noiseSuppression:false, autoGainControl:false } }); audioState.context = new (window.AudioContext || window.webkitAudioContext)(); await audioState.context.resume(); audioState.source=audioState.context.createMediaStreamSource(audioState.stream); audioState.gainNode=audioState.context.createGain(); audioState.preampNode=audioState.context.createGain(); audioState.analyser=audioState.context.createAnalyser(); audioState.analyser.fftSize=2048; audioState.destination=audioState.context.createMediaStreamDestination(); audioState.source.connect(audioState.gainNode); audioState.gainNode.connect(audioState.preampNode); audioState.preampNode.connect(audioState.analyser); audioState.preampNode.connect(audioState.destination); audioState.monitorGainNode=audioState.context.createGain(); audioState.monitorGainNode.gain.value=0; audioState.preampNode.connect(audioState.monitorGainNode); audioState.monitorGainNode.connect(audioState.context.destination); if (audioState.gainNode) audioState.gainNode.gain.value=1; updateInputGain(); updateMonitorGain(); startMeterLoop(); }
function chooseRecorderMimeType() { const candidates=['audio/webm;codecs=opus','audio/webm']; return candidates.find((type)=>window.MediaRecorder?.isTypeSupported?.(type)) || ''; }
function getAudioFileInfo(mimeType) { const type=String(mimeType || '').toLowerCase(); if (type.includes('wav')) return { mimeType:'audio/wav', ext:'wav' }; if (type.includes('mp4') || type.includes('m4a')) return { mimeType:'audio/mp4', ext:'m4a' }; return { mimeType:'audio/webm', ext:'webm' }; }
async function postAudioChunk(blob) { if (!currentEvent || !blob || blob.size < 3500) return; const detectedType=blob.type || audioState.mimeType || 'audio/webm'; const fileInfo=getAudioFileInfo(detectedType); const form=new FormData(); form.append('code', currentEvent.adminCode); form.append('audio', new File([blob], `chunk.${fileInfo.ext}`, { type:fileInfo.mimeType })); const res=await fetch(`/api/events/${currentEvent.id}/transcribe`, { method:'POST', body: form }); const data=await res.json(); if (!data.ok) throw new Error(data.error || 'Nu am putut transcrie audio.'); }
function enqueueAudioBlob(blob) { if (!blob || blob.size < 3500) return; audioState.uploadQueue.push(blob); if (!audioState.busy) drainAudioUploadQueue().catch(console.error); }
async function drainAudioUploadQueue() { if (audioState.busy) return; audioState.busy=true; try { while (audioState.uploadQueue.length) { const blob=audioState.uploadQueue.shift(); try { await postAudioChunk(blob); } catch (err) { console.error(err); setStatus(err.message || 'Eroare la trimiterea audio.'); } } } finally { audioState.busy=false; } }
async function startTranslation() { if (!currentEvent) return alert('Alege sau creează întâi un eveniment.'); await syncSpeedToEvent(); if (!window.MediaRecorder) return alert('Browserul nu suportă MediaRecorder.'); try { await createAudioPipeline(); } catch (err) { console.error(err); setStatus('Nu am putut porni audio.'); return; } const mimeType=chooseRecorderMimeType(); if (!mimeType) return alert('Browserul nu suportă audio/webm.'); audioState.running=true; audioState.mimeType=mimeType; window.isRecognitionRunning=true; switchTab('live'); setOnAirState(true); await enableScreenWakeLock(); const startRecorderCycle=()=>{ if (!audioState.running) return; audioState.chunks=[]; const recorder=new MediaRecorder(audioState.destination.stream, { mimeType, audioBitsPerSecond:128000 }); audioState.recorder=recorder; recorder.ondataavailable=(event)=>{ if (event.data && event.data.size > 0) audioState.chunks.push(event.data); }; recorder.onstop=()=>{ const finalType=recorder.mimeType || mimeType || 'audio/webm'; const blob=new Blob(audioState.chunks, { type: finalType }); audioState.chunks=[]; if (audioState.chunkTimer) { clearTimeout(audioState.chunkTimer); audioState.chunkTimer=null; } if (audioState.recorder===recorder) audioState.recorder=null; if (audioState.running) startRecorderCycle(); if (blob.size >= 3500) enqueueAudioBlob(blob); }; recorder.start(); audioState.chunkTimer=setTimeout(()=>{ if (recorder.state==='recording') recorder.stop(); }, 5200); }; startRecorderCycle(); setStatus('On-Air. Traduce din sursa selectată.'); }
async function stopTranslation() { audioState.running=false; window.isRecognitionRunning=false; if (audioState.chunkTimer) { clearTimeout(audioState.chunkTimer); audioState.chunkTimer=null; } setOnAirState(false); await disableScreenWakeLock(); if (audioState.recorder && audioState.recorder.state==='recording') { audioState.recorder.stop(); setTimeout(()=>{ destroyAudioPipeline().catch(console.error); }, 100); return; } await destroyAudioPipeline(); setStatus('Oprit.'); }
function sendManualText() { if (!currentEvent) return alert('Alege sau creează întâi un eveniment.'); const text=$('manualText')?.value.trim(); if (!text) return; socket.emit('submit_text', { eventId: currentEvent.id, text }); $('manualText').value=''; lastManualEnterAt=0; }

socket.on('joined_event', ({ event, role }) => { if (role !== 'admin') return; currentEvent=event; $('speed').value=event.speed || 'balanced'; currentVolume=event.audioVolume; currentMuted=event.audioMuted; $('volumeRange').value=String(currentVolume); $('audioStateLabel').textContent=currentMuted ? 'Audio global oprit.' : 'Audio global activ.'; $('transcriptList').innerHTML=''; (event.transcripts || []).forEach(renderEntry); fillGlossaryLangs(currentEvent.targetLangs || []); renderActiveEventBadge(currentEvent); renderSongStatus(currentEvent.songState); closeInlineEditors(); refreshEventList(); refreshSongLibrary(); $('partialTranscript').textContent='Aștept propoziția completă...'; $('participantLink').value=event.participantLink || ''; $('displayLink').value=event.displayLink || event.songLink || ''; $('qrImage').src=event.qrCodeDataUrl || ''; $('adminCode').textContent=event.adminCode || '-'; });
socket.on('transcript_entry', (entry) => { if (!currentEvent) return; currentEvent.transcripts = currentEvent.transcripts || []; const exists=getEntryById(entry.id); if (!exists) currentEvent.transcripts.push(entry); renderEntry(entry); $('partialTranscript').textContent='Aștept propoziția completă...'; });
socket.on('transcript_updated', updateEntry);
socket.on('transcript_source_updated', (payload) => { updateSourceEntry(payload); $('partialTranscript').textContent='Aștept propoziția completă...'; });
socket.on('audio_state', ({ audioMuted, audioVolume }) => { currentMuted=audioMuted; currentVolume=audioVolume; $('volumeRange').value=String(audioVolume); $('audioStateLabel').textContent=audioMuted ? 'Audio global oprit.' : 'Audio global activ.'; });
socket.on('partial_transcript', ({ text }) => { $('partialTranscript').textContent = text || 'Aștept propoziția completă...'; });
socket.on('participant_stats', renderParticipantStats);
socket.on('server_error', ({ message }) => setStatus(message || 'Eroare server.'));
socket.on('active_event_changed', async ({ eventId }) => { if (currentEvent) { currentEvent.isActive = currentEvent.id === eventId; renderActiveEventBadge(currentEvent); } await refreshEventList(); try { const res=await fetch('/api/events/active'); const data=await res.json(); if (data.ok && data.event) await openEventById(data.event.id); else resetParticipantStats(); } catch (_) {} });
socket.on('song_state', (songState) => { if (!currentEvent) return; currentEvent.songState=songState; renderSongStatus(songState); });
socket.on('mode_changed', ({ mode }) => { if (!currentEvent) return; currentEvent.mode=mode; setStatus(`Mod curent: ${mode}`); });

$('createEventBtn')?.addEventListener('click', createEvent);
$('sendManualBtn')?.addEventListener('click', sendManualText);
$('speed')?.addEventListener('change', syncSpeedToEvent);
$('openTranscriptTabBtn')?.addEventListener('click', () => switchTab('transcript'));
$('manualText')?.addEventListener('keydown', (e)=>{ if (e.key !== 'Enter' || e.shiftKey) return; const now=Date.now(); if (now-lastManualEnterAt < 600) { e.preventDefault(); sendManualText(); return; } lastManualEnterAt=now; });
$('saveGlossaryBtn')?.addEventListener('click', async ()=>{ if (!currentEvent) return alert('Alege sau creează întâi un eveniment.'); const source=$('glossarySource')?.value.trim(); const target=$('glossaryTarget')?.value.trim(); const lang=$('glossaryLang')?.value; const permanent=!!$('glossaryPermanent')?.checked; if (!source || !target) return; const res=await fetch(`/api/events/${currentEvent.id}/glossary`, { method:'POST', headers:{ 'Content-Type':'application/json' }, body: JSON.stringify({ source, target, lang, permanent }) }); const data=await res.json(); if (data.ok) { $('glossarySource').value=''; $('glossaryTarget').value=''; setStatus('Traducerea a fost salvată în glosar.'); } });
$('saveSourceCorrectionBtn')?.addEventListener('click', async ()=>{ if (!currentEvent) return alert('Alege sau creează întâi un eveniment.'); const heard=$('sourceWrong')?.value.trim(); const correct=$('sourceCorrect')?.value.trim(); const permanent=!!$('sourceCorrectionPermanent')?.checked; if (!heard || !correct) return; const res=await fetch(`/api/events/${currentEvent.id}/source-corrections`, { method:'POST', headers:{ 'Content-Type':'application/json' }, body: JSON.stringify({ heard, correct, permanent }) }); const data=await res.json(); if (data.ok) { $('sourceWrong').value=''; $('sourceCorrect').value=''; setStatus('Corecția de recunoaștere a fost salvată.'); } });
$('glossaryMode')?.addEventListener('change', updateGlossaryMode);
$('muteGlobalBtn')?.addEventListener('click', ()=>{ if (!currentEvent) return; currentMuted=!currentMuted; socket.emit('set_audio_state', { eventId: currentEvent.id, audioMuted: currentMuted, audioVolume: currentVolume, code: currentEvent.adminCode }); });
$('panicBtn')?.addEventListener('click', ()=>{ if (!currentEvent) return; currentMuted=true; currentVolume=0; $('volumeRange').value='0'; socket.emit('set_audio_state', { eventId: currentEvent.id, audioMuted:true, audioVolume:0, code: currentEvent.adminCode }); });
$('volumeRange')?.addEventListener('input', ()=>{ currentVolume=Number($('volumeRange')?.value || 70); if (!currentEvent) return; socket.emit('set_audio_state', { eventId: currentEvent.id, audioMuted:currentMuted, audioVolume:currentVolume, code: currentEvent.adminCode }); });
$('inputGainRange')?.addEventListener('input', updateInputGain); $('monitorAudioBox')?.addEventListener('change', updateMonitorGain); $('monitorGainRange')?.addEventListener('input', updateMonitorGain);
$('audioInput')?.addEventListener('change', async ()=>{ if (audioState.running) await startTranslation(); else { try { await createAudioPipeline(); setStatus('Sursa audio a fost schimbată.'); } catch (err) { console.error(err); setStatus('Nu am putut porni sursa selectată.'); } } });
$('startRecognitionBtn')?.addEventListener('click', startTranslation); $('stopRecognitionBtn')?.addEventListener('click', stopTranslation); $('copyParticipantBtn')?.addEventListener('click', ()=>copyField('participantLink','copyParticipantBtn')); $('copyDisplayBtn')?.addEventListener('click', ()=>copyField('displayLink','copyDisplayBtn')); $('setActiveEventBtn')?.addEventListener('click', setActiveEvent); $('refreshEventsBtn')?.addEventListener('click', refreshEventList); $('jumpLiveBtn')?.addEventListener('click', ()=>{ sourceEditLock=false; closeInlineEditors(); switchTab('transcript'); });
$('setLiveModeBtn')?.addEventListener('click', ()=>setMode('live')); $('setSongModeBtn')?.addEventListener('click', ()=>setMode('song')); $('loadSongBtn')?.addEventListener('click', loadSong); $('clearSongBtn')?.addEventListener('click', ()=>songAction('/song/clear')); $('songPrevBtn')?.addEventListener('click', ()=>songAction('/song/prev')); $('songNextBtn')?.addEventListener('click', ()=>songAction('/song/next'));
$('eventList')?.addEventListener('click', async (e) => { const btn=e.target.closest('button[data-action]'); if (!btn) return; const id=btn.getAttribute('data-id'); const action=btn.getAttribute('data-action'); if (!id || !action) return; if (action==='open') return openEventById(id); if (action==='activate') { const res=await fetch(`/api/events/${id}/activate`, { method:'POST' }); const data=await res.json(); if (data.ok) { if (currentEvent && currentEvent.id===id) { currentEvent=data.event; renderActiveEventBadge(currentEvent); } await refreshEventList(); setStatus('Evenimentul ales este acum live pentru participanți.'); } return; } if (action==='delete') { if (!confirm('Ștergi definitiv acest eveniment?')) return; const res=await fetch(`/api/events/${id}`, { method:'DELETE' }); const data=await res.json(); if (data.ok) { if (currentEvent?.id===id) { currentEvent=null; $('adminCode').textContent='-'; $('participantLink').value=''; $('displayLink').value=''; $('qrImage').src=''; $('transcriptList').innerHTML=''; renderActiveEventBadge(null); resetParticipantStats(); $('partialTranscript').textContent='Aștept propoziția completă...'; } await refreshEventList(); setStatus('Evenimentul a fost șters.'); } } });
$('songLibraryList')?.addEventListener('click', async (e)=>{ const btn=e.target.closest('button[data-song-action]'); if (!btn || !currentEvent) return; const songId=btn.getAttribute('data-song-id'); const action=btn.getAttribute('data-song-action'); if (action==='load') { const res=await fetch(`/api/events/${currentEvent.id}/song/library/${songId}/load`, { method:'POST' }); const data=await res.json(); if (data.ok) { currentEvent=data.event; renderSongStatus(data.songState); setStatus('Cântarea salvată a fost încărcată.'); } return; } if (action==='delete') { const res=await fetch(`/api/events/${currentEvent.id}/song/library/${songId}`, { method:'DELETE' }); const data=await res.json(); if (data.ok) refreshSongLibrary(); } });

document.addEventListener('visibilitychange', async ()=>{ if (document.visibilityState==='visible' && window.isRecognitionRunning && !screenWakeLock) await enableScreenWakeLock(); });
window.addEventListener('beforeunload', async ()=>{ await disableScreenWakeLock(); });
window.addEventListener('load', async ()=>{ createLanguageChecks(); const now=new Date(); if ($('eventDate')) $('eventDate').value=now.toISOString().slice(0,10); if ($('eventTime')) $('eventTime').value=`${String(now.getHours()).padStart(2,'0')}:${String(now.getMinutes()).padStart(2,'0')}`; try { await navigator.mediaDevices.getUserMedia({ audio:true }); } catch (_) {} await loadAudioInputs(); initTabs(); updateGlossaryMode(); updateInputGain(); updateMonitorGain(); setOnAirState(false); resetParticipantStats(); await refreshEventList(); try { const res=await fetch('/api/events/active'); const data=await res.json(); if (data.ok && data.event) await openEventById(data.event.id); } catch (_) {} });
